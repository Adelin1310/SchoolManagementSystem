const host = 'https://localhost:7055/api/'
export const schoolHost = host + 'School/'
export const studentHost = host + 'Student/'
export const classHost = host + 'Class/'
export const classbookHost = host + 'Classbook/'
export const teacherHost = host + 'Teacher/'
export const subjectHost = host + 'Subject/'

const dataFolder = 'localhost:8887/';

export const photosFolder = dataFolder + 'photos/';